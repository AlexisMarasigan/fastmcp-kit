"""E2E test fixtures.

Real fixtures (`running_server`, `mcp_client`, `terminate_cleanly`)
land in sprint 5 with the end-to-end suite. Today this file only
exists so `docs/TESTING.md` doesn't reference a missing path.
"""

from __future__ import annotations
