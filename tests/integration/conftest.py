"""Integration test fixtures.

Integration tests assume `make stack-up` has brought the compose stack
to life. Real fixtures (`compose_stack`, `prom_client`, `grafana_client`)
land in sprint 3 with the observability domain's actual implementation.
"""

from __future__ import annotations
